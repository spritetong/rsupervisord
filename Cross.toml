# Pin Linux glibc builds to the 2.28 baseline (Node.js / RHEL 8 / Debian 10)
# so release binaries run on distros with glibc >= 2.28.
# musl targets are static and unaffected.

[target.x86_64-unknown-linux-gnu]
zig = "2.28"

[target.aarch64-unknown-linux-gnu]
zig = "2.28"
